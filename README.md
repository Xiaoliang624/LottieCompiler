
  # Cross-platform UI adaptation

  This is a code bundle for Cross-platform UI adaptation.
  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  
